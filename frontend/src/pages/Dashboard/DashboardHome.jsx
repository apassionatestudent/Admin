import React from 'react';

// => Default landing page when admin hits /dashboard
// => Fill this in later with summary cards, charts, reminders, etc.
// TODO: replace this page with all quick enrollment information, if God willing. 
export default function DashboardHome() {
    return (
        <main style={{ padding: '32px' }}>
            <h2>Dashboard</h2>
            <p>Welcome to the admin dashboard. Content coming soon.</p>
        </main>
    );
}